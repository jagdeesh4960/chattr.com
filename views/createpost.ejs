<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>New Post | Chattr</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-black text-white min-h-screen flex flex-col">

  <!-- Brand casual placement -->
  <div class="text-center py-4">
    <h1 class="text-3xl font-extrabold text-pink-600 tracking-wide">Chattr</h1>
    <p class="text-sm text-gray-400">Share your moments with style.</p>
  </div>
  <a class="px-3 py-1 rounded-lg bg-red-600  text-white text-sm absolute right-2 top-2" href="/home">cancle</a>

  <!-- Form container -->
  <div class="flex-grow flex justify-center items-center px-4">
    <form 
      action="/post/create" 
      method="POST" 
      enctype="multipart/form-data"
      class="w-full max-w-md bg-pink-100 p-6 rounded-lg shadow-lg text-black space-y-4"
    >

      <h2 class="text-2xl font-bold text-pink-600 text-center mb-2">Create a New Post</h2>

      <!-- Image upload -->
      <div>
        <label class="block font-medium mb-1">Upload Image</label>
        <input 
          type="file" 
          name="image" 
          accept="image/*"
          class="w-full px-3 py-2 bg-white rounded focus:outline-none focus:ring-2 focus:ring-pink-500"
          required
        />
      </div>

      <!-- Caption -->
      <div>
        <label class="block font-medium mb-1">Caption</label>
        <textarea 
          name="caption" 
          rows="3" 
          placeholder="Write something cool..."
          class="w-full px-3 py-2 bg-white rounded focus:outline-none focus:ring-2 focus:ring-pink-500 resize-none"
          required
        ></textarea>
      </div>

      <!-- Submit -->
      <button 
        type="submit" 
        class="w-full bg-pink-600 text-white py-2 rounded hover:bg-pink-700 transition duration-200"
      >
        Post It ✨
      </button>

    </form>
   
  </div>

  <!-- Footer -->
  <footer class="bg-pink-600 text-center text-white text-sm py-3 mt-6">
    © 2025 Chattr — Stay bold. Stay social.
  </footer>

</body>
</html>
